<div class="container">
  <div class="modal-body row">
  <div class="col-md-6">
    <%= cl_image_tag @item.style_photo.key, width: 400, height: 700, crop: :fill, :class => 'rounded' %>
    <h1><%= @item.title %></h1>
    <p><%= @item.description %></p>
  </div>
  <div class="col-md-3">
    <%= cl_image_tag @item.individual_pieces[0].key, height: 300, width: 300, crop: :fill, :class => 'img-thumbnail m-2' %>
    <%= cl_image_tag @item.individual_pieces[1].key, height: 300, width: 300, crop: :fill, :class => 'img-thumbnail m-2' %>
    <h4>Price per night <%= @item.price%>€</h4>
  </div>
  <div class="col-md-3">
    <%= cl_image_tag @item.individual_pieces[2].key, height: 300, width: 300, crop: :fill, :class => 'img-thumbnail m-2' %>
    <%= cl_image_tag @item.individual_pieces[3].key, height: 300, width: 300, crop: :fill, :class => 'img-thumbnail m-2' %>
    <%= link_to 'Borrow now', new_item_booking_path(@item), :class => 'btn btn-success' %>
  </div>
  </div>
</div>
